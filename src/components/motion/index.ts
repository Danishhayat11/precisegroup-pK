/**
 * Motion primitives barrel. Import from `@/components/motion` so every
 * surface shares the same choreography language and design tokens.
 *
 *   import { Reveal, Stagger, GlowCard, PendingSurface } from "@/components/motion";
 */
export { PageTransition } from "./PageTransition";
export { RouteProgress } from "./RouteProgress";
export { MotionSection, MotionItem } from "./MotionSection";
export { Reveal, Stagger, StaggerItem } from "./Reveal";
export { GlowCard } from "./GlowCard";
export { PendingSurface, PendingInline } from "./PendingSurface";
